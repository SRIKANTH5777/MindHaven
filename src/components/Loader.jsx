import React from 'react';
import './Loader.css';

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
    <p>Loading books...</p>
  </div>
);

export default Loader;